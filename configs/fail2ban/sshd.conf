# ATLAS PLATFORM — fail2ban SSH jail
# /etc/fail2ban/jail.d/atlas-sshd.conf

[sshd]
enabled  = true
port     = 2222
filter   = sshd
logpath  = %(sshd_log)s
backend  = %(sshd_backend)s
maxretry = 3
findtime = 10m
bantime  = 1h
ignoreip = 127.0.0.1/8 ::1
